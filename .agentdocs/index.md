# LabFlow 代理文档索引

## 用途

本目录用于沉淀仅面向 AI 代理的项目背景、技术约束、阶段任务与长期有效的实现共识，避免后续重复扫描仓库。

## 全局约束

- 项目定位：华科 AI 大赛参赛作品，目标是构建“论文代码关联阅读与实验室协作助手”。
- 开发语言：Python 3.10+。
- 核心能力闭环：Perceive（PDF 与 Git 仓库解析） -> Reason（论文与代码对齐推理） -> Act（生成审计/协作周报）。
- UI 默认使用 Streamlit。
- PDF 解析优先使用 PyMuPDF。
- Agent 逻辑必须自研，不得直接套用第三方现成 Agent 成品平台。
- LLM 调用必须兼容 OpenAI 风格接口；当前已提供七牛兼容端点与模型配置。
- 代码、注释、提交信息、文档均不得出现 AI 生成痕迹。
- 每完成一个子功能应提醒用户进行一次小粒度 Git Commit。
- 所有沟通、代码注释、代理文档统一使用中文；必要时可保留英文术语并附简要中文说明。
- 单文件不超过 1000 行，优先通过模块拆分控制复杂度。

## 读取指引

### 前端文档

- `.agentdocs/frontend/architecture.md`
  - 读取场景：修改 Streamlit 页面结构、状态组织或展示辅助函数前必读。
  - 主要内容：前端模块职责划分、页面状态边界、展示逻辑复用原则与测试要求。
- `.agentdocs/frontend/ui-design.md`
  - 读取场景：调整首页、工作区或结果展示视觉样式前必读。
  - 主要内容：LabFlow 的界面风格、交互原则与“源码导览”设计约束。

### 后端文档

- `.agentdocs/backend/architecture.md`
  - 读取场景：修改推理层、解析层或结果模型结构前必读。
  - 主要内容：后端模块边界、推理结果建模约束、候选收束原则与测试要求。

### 当前任务文档

- `.agentdocs/workflow/260319-project-bootstrap-planning.md`
  - 读取场景：开始项目规划、初始化工程骨架、确认阶段目标前必读。
  - 主要内容：当前仓库状态、比赛约束整理、推荐架构、分阶段实施计划、待确认事项。
- `.agentdocs/workflow/260322-refactor-agent-runtime.md`
  - 读取场景：重构推理层 Agent 运行时、拆分 Prompt / Tool / Engine 架构前必读。
  - 主要内容：当前推理层问题、LangChain 风格三层解耦目标、实施阶段与兼容迁移约束。
- `.agentdocs/workflow/260322-modular-agent-orchestration.md`
  - 读取场景：继续把推理层拆成 Planner + 多子 Agent，并恢复代码对齐链可见性时必读。
  - 主要内容：模块化子 Agent 方案、Planner 决策字段、源码展示阈值调整与测试范围。
- `.agentdocs/workflow/260324-quick-guide-page.md`
  - 读取场景：把首页“快速导读”从内联卡片改为独立页面入口时必读。
  - 主要内容：导读页独立化目标、路由改造范围、首页入口收敛方式与验证要求。

## 仓库外层输入资料

以下文件位于仓库根目录，不属于代理文档体系，但目前是项目启动阶段的核心输入：

- `.env`
  - 读取场景：确认运行所需环境变量是否存在时。
  - 注意事项：仅识别变量名称与完整性，不在回复、日志、代码注释中泄露密钥值。

## 文档维护规则

- 新增文档必须先判断是否可并入现有文档，避免重复沉淀。
- 更新文档时按原有结构重组内容，不做无上下文的末尾追加。
- 仅记录长期有效或当前复杂任务确实需要的内容，不写工作汇报式文档。
